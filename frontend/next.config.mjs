/** @type {import('next').NextConfig} */
const nextConfig = {
    // Output standalone for docker optimization if needed, 
    // but for dev 'standalone' is not strictly required.
    // output: 'standalone', 
};

export default nextConfig;
